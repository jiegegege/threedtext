function fontChang(str){
    var num=str.length;
    a[num*16][num*16];
    var arr =  new Array(16*num);   //表格的行
    for(var i = 0;i < arr.length; i++){
        arr[i] = new Array(16);     //每行的列
    }
    for(var i=0;i<arr.length;i++){
        for(var j=0;j<arr[i].length;j++){
            arr[i][j]=0;
        }
    }
}